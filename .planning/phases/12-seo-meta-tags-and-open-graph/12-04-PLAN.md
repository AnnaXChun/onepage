---
phase: 12-seo-meta-tags-and-open-graph
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Editor/EditorToolbar.tsx
  - frontend/src/components/Editor/SEOPanel.tsx
  - frontend/src/services/api.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-04

must_haves:
  truths:
    - "User can set meta title and description via SEO panel"
    - "User can preview how link will appear on social media"
    - "User can save custom robots.txt for their site"
    - "SEO panel has two tabs: Meta Tags and robots.txt"
  artifacts:
    - path: "frontend/src/components/Editor/SEOPanel.tsx"
      provides: "SEO settings slide-over panel"
      contains: "Meta Tags.*robots.txt.*OG Preview"
    - path: "frontend/src/components/Editor/EditorToolbar.tsx"
      provides: "SEO button in editor toolbar"
      contains: "SEO.*icon"
    - path: "frontend/src/services/api.ts"
      provides: "API methods for SEO settings"
      exports: ["updateBlogSeo", "updateRobotsTxt"]
  key_links:
    - from: "frontend/src/components/Editor/SEOPanel.tsx"
      to: "frontend/src/services/api.ts"
      via: "updateBlogSeo and updateRobotsTxt API calls"
    - from: "frontend/src/components/Editor/EditorToolbar.tsx"
      to: "frontend/src/components/Editor/SEOPanel.tsx"
      via: "SEO button click opens panel"
---

<objective>
Build the frontend SEO panel per UI-SPEC: slide-over from EditorToolbar with two tabs (Meta Tags and robots.txt), OG Preview card, amber warning about re-publish, auto-save with 500ms debounce for meta fields, explicit Save button for robots.txt.
</objective>

<context>
@frontend/src/components/Editor/EditorToolbar.tsx
@frontend/src/components/Editor/BlockConfigPanel.tsx
@frontend/src/services/api.ts

**UI-SPEC Key Points:**
- SEO panel is w-80 (320px) slide-over from right
- Two tabs: "Meta Tags" and "robots.txt"
- Meta Tags tab: meta title input (max 60), meta description textarea (max 160), amber warning, OG Preview card
- robots.txt tab: textarea (monospace), Save button, Reset to default button
- Auto-save for meta fields after 500ms debounce
- Explicit Save for robots.txt (not auto-save)
- Animation: 300ms ease-out-quart slide in, 250ms slide out
- No backdrop - editor remains interactive behind
- Close via X button or Escape key

**Design tokens from UI-SPEC:**
- Font: Plus Jakarta Sans
- Colors: oklch palette from global.css
- Accent for focus rings: oklch(55% 0.12 275)
- Amber warning: bg-amber-500/10 text-amber-400 border-amber-500/20

**OG Preview card structure:**
```
[Cover image 16:9 or placeholder]
Site Title (truncated)
site.com/host/username
Description text truncated...
[OG] [X badges]
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEO API methods to api.ts</name>
  <files>frontend/src/services/api.ts</files>
  <read_first>
    frontend/src/services/api.ts
  </read_first>
  <action>
    Add the following API methods to api.ts:

```typescript
/**
 * Update SEO settings for a blog.
 * PUT /api/blog/{id}/seo
 * SEO-01
 */
export const updateBlogSeo = async (blogId: number, seoData: {
  metaTitle?: string;
  metaDescription?: string;
}): Promise<void> => {
  const response = await axios.put(`${API_BASE_URL}/blog/${blogId}/seo`, seoData);
  return response.data;
};

/**
 * Get blog SEO settings.
 * GET /api/blog/{id}
 * SEO-01
 */
export const getBlogSeo = async (blogId: number): Promise<{
  metaTitle?: string;
  metaDescription?: string;
}> => {
  const response = await axios.get(`${API_BASE_URL}/blog/${blogId}`);
  const blog = response.data.data;
  return {
    metaTitle: blog.metaTitle,
    metaDescription: blog.metaDescription,
  };
};

/**
 * Update user robots.txt content.
 * PUT /api/user/robots
 * SEO-03
 */
export const updateRobotsTxt = async (robotsTxt: string): Promise<void> => {
  const response = await axios.put(`${API_BASE_URL}/user/robots`, { robotsTxt });
  return response.data;
};
```

Note: The blog already has metaTitle and metaDescription fields in the GET response, so no new endpoint needed.
  </action>
  <verify>
    <automated>grep -c "updateBlogSeo" frontend/src/services/api.ts && grep -c "updateRobotsTxt" frontend/src/services/api.ts</automated>
  </verify>
  <done>SEO API methods added to api.ts including updateRobotsTxt</done>
</task>

<task type="auto">
  <name>Task 2: Create SEOPanel.tsx component</name>
  <files>frontend/src/components/Editor/SEOPanel.tsx</files>
  <action>
    Create new file `frontend/src/components/Editor/SEOPanel.tsx`:

```tsx
import { useState, useEffect, useCallback } from 'react';
import { updateBlogSeo, updateRobotsTxt } from '@/services/api';

interface SEOPanelProps {
  blogId: number;
  initialMetaTitle?: string;
  initialMetaDescription?: string;
  username?: string;
  coverImage?: string;
  siteUrl?: string;
  isOpen: boolean;
  onClose: () => void;
}

const MAX_TITLE_LENGTH = 60;
const MAX_DESCRIPTION_LENGTH = 160;

export default function SEOPanel({
  blogId,
  initialMetaTitle = '',
  initialMetaDescription = '',
  username = 'user',
  coverImage,
  siteUrl = 'http://localhost:8080',
  isOpen,
  onClose,
}: SEOPanelProps) {
  const [activeTab, setActiveTab] = useState<'meta' | 'robots'>('meta');
  const [metaTitle, setMetaTitle] = useState(initialMetaTitle);
  const [metaDescription, setMetaDescription] = useState(initialMetaDescription);
  const [robotsTxt, setRobotsTxt] = useState(
    `User-agent: *\nAllow: /\nSitemap: ${siteUrl}/host/${username}/sitemap.xml`
  );
  const [isSaving, setIsSaving] = useState(false);
  const [saveError, setSaveError] = useState<string | null>(null);
  const [robotsSaveError, setRobotsSaveError] = useState<string | null>(null);

  // Auto-save meta fields with 500ms debounce
  useEffect(() => {
    if (!isOpen || activeTab !== 'meta') return;

    const timer = setTimeout(async () => {
      try {
        setIsSaving(true);
        setSaveError(null);
        await updateBlogSeo(blogId, { metaTitle, metaDescription });
      } catch (error) {
        setSaveError('Failed to save. Check your connection and try again.');
        console.error('SEO save error:', error);
      } finally {
        setIsSaving(false);
      }
    }, 500);

    return () => clearTimeout(timer);
  }, [blogId, metaTitle, metaDescription, isOpen, activeTab]);

  // Handle Escape key
  useEffect(() => {
    const handleEscape = (e: KeyboardEvent) => {
      if (e.key === 'Escape' && isOpen) {
        onClose();
      }
    };
    window.addEventListener('keydown', handleEscape);
    return () => window.removeEventListener('keydown', handleEscape);
  }, [isOpen, onClose]);

  const handleSaveRobots = async () => {
    try {
      setRobotsSaveError(null);
      await updateRobotsTxt(robotsTxt);
    } catch (error) {
      setRobotsSaveError('Failed to save robots.txt. Check your connection and try again.');
      console.error('Robots.txt save error:', error);
    }
  };

  const handleResetRobots = () => {
    setRobotsTxt(`User-agent: *\nAllow: /\nSitemap: ${siteUrl}/host/${username}/sitemap.xml`);
  };

  if (!isOpen) return null;

  return (
    <div
      className="fixed right-0 top-0 h-full w-80 bg-[oklch(20%_0.015_260)] border-l border-[oklch(30%_0.015_260)] shadow-xl z-50"
      style={{
        animation: 'slideInRight 300ms ease-out-quart',
      }}
    >
      {/* Header */}
      <div className="flex items-center justify-between px-4 py-3 border-b border-[oklch(30%_0.015_260)]">
        <h2 className="text-[15px] font-semibold text-[oklch(90%_0_0)]">SEO Settings</h2>
        <button
          onClick={onClose}
          className="p-1 text-[oklch(55%_0_0)] hover:text-[oklch(90%_0_0)] transition-colors"
          aria-label="Close SEO settings"
        >
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
            <path d="M15 5L5 15M5 5l10 10" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/>
          </svg>
        </button>
      </div>

      {/* Tab Navigation */}
      <div className="flex border-b border-[oklch(30%_0.015_260)]">
        <button
          onClick={() => setActiveTab('meta')}
          className={`flex-1 px-4 py-2.5 text-[13px] font-medium transition-colors ${
            activeTab === 'meta'
              ? 'text-[oklch(55%_0.12_275)] border-b-2 border-[oklch(55%_0.12_275)]'
              : 'text-[oklch(55%_0_0)] hover:text-[oklch(80%_0_0)]'
          }`}
        >
          Meta Tags
        </button>
        <button
          onClick={() => setActiveTab('robots')}
          className={`flex-1 px-4 py-2.5 text-[13px] font-medium transition-colors ${
            activeTab === 'robots'
              ? 'text-[oklch(55%_0.12_275)] border-b-2 border-[oklch(55%_0.12_275)]'
              : 'text-[oklch(55%_0_0)] hover:text-[oklch(80%_0_0)]'
          }`}
        >
          robots.txt
        </button>
      </div>

      {/* Tab Content */}
      <div className="p-4 overflow-y-auto h-[calc(100%-120px)]">
        {activeTab === 'meta' ? (
          <div className="space-y-4">
            {/* Amber Warning */}
            <div className="bg-amber-500/10 text-amber-400 border border-amber-500/20 rounded-lg px-3 py-2 text-xs">
              Re-publish your site for SEO changes to take effect.
            </div>

            {/* Meta Title */}
            <div>
              <label className="block text-[13px] font-medium text-[oklch(80%_0_0)] mb-1.5">
                Meta Title
              </label>
              <div className="relative">
                <input
                  type="text"
                  value={metaTitle}
                  onChange={(e) => setMetaTitle(e.target.value.slice(0, MAX_TITLE_LENGTH))}
                  placeholder="Enter a search-friendly title"
                  maxLength={MAX_TITLE_LENGTH}
                  className="w-full px-3 py-2 bg-[oklch(15%_0.015_260)] border border-[oklch(30%_0.015_260)] rounded-lg text-[14px] text-[oklch(90%_0_0)] placeholder:text-[oklch(40%_0_0)] focus:outline-none focus:ring-2 focus:ring-[oklch(55%_0.12_275)] focus:border-transparent transition-shadow"
                />
                <span className="absolute right-3 top-1/2 -translate-y-1/2 text-[12px] text-[oklch(40%_0_0)]">
                  {metaTitle.length}/{MAX_TITLE_LENGTH}
                </span>
              </div>
            </div>

            {/* Meta Description */}
            <div>
              <label className="block text-[13px] font-medium text-[oklch(80%_0_0)] mb-1.5">
                Meta Description
              </label>
              <div className="relative">
                <textarea
                  value={metaDescription}
                  onChange={(e) => setMetaDescription(e.target.value.slice(0, MAX_DESCRIPTION_LENGTH))}
                  placeholder="Describe your site for search engines"
                  maxLength={MAX_DESCRIPTION_LENGTH}
                  rows={3}
                  className="w-full px-3 py-2 bg-[oklch(15%_0.015_260)] border border-[oklch(30%_0.015_260)] rounded-lg text-[14px] text-[oklch(90%_0_0)] placeholder:text-[oklch(40%_0_0)] focus:outline-none focus:ring-2 focus:ring-[oklch(55%_0.12_275)] focus:border-transparent transition-shadow resize-none"
                />
                <span className="absolute right-3 bottom-2 text-[12px] text-[oklch(40%_0_0)]">
                  {metaDescription.length}/{MAX_DESCRIPTION_LENGTH}
                </span>
              </div>
            </div>

            {/* Error Message */}
            {saveError && (
              <p className="text-xs text-red-400">{saveError}</p>
            )}

            {/* OG Preview Card */}
            <div className="mt-6">
              <h3 className="text-[13px] font-medium text-[oklch(80%_0_0)] mb-2">Social Preview</h3>
              <OGPreviewCard
                title={metaTitle || 'Site Title'}
                description={metaDescription || 'Description text will appear here...'}
                image={coverImage}
                url={`${siteUrl}/host/${username}`}
              />
            </div>
          </div>
        ) : (
          <div className="space-y-4">
            {/* robots.txt Editor */}
            <div>
              <label className="block text-[13px] font-medium text-[oklch(80%_0_0)] mb-1.5">
                robots.txt content
              </label>
              <textarea
                value={robotsTxt}
                onChange={(e) => setRobotsTxt(e.target.value)}
                rows={12}
                className="w-full px-3 py-2 bg-[oklch(15%_0.015_260)] border border-[oklch(30%_0.015_260)] rounded-lg text-[14px] text-[oklch(90%_0_0)] font-mono placeholder:text-[oklch(40%_0_0)] focus:outline-none focus:ring-2 focus:ring-[oklch(55%_0.12_275)] focus:border-transparent transition-shadow resize-none"
                placeholder="User-agent: * / Allow: / Sitemap: ..."
              />
              <p className="mt-2 text-[12px] text-[oklch(55%_0_0)]">
                User-agent: * / Allow: / Sitemap: {siteUrl}/host/{username}/sitemap.xml
              </p>
            </div>

            {/* Error Message */}
            {robotsSaveError && (
              <p className="text-xs text-red-400">{robotsSaveError}</p>
            )}

            {/* Save and Reset Buttons */}
            <div className="flex gap-2 pt-2">
              <button
                onClick={handleSaveRobots}
                className="flex-1 px-4 py-2 bg-[oklch(55%_0.12_275)] text-white rounded-lg text-[14px] font-medium hover:opacity-90 transition-opacity"
              >
                Save
              </button>
              <button
                onClick={handleResetRobots}
                className="px-4 py-2 bg-[oklch(30%_0.015_260)] text-[oklch(80%_0_0)] rounded-lg text-[14px] font-medium hover:bg-[oklch(35%_0.015_260)] transition-colors"
              >
                Reset to default
              </button>
            </div>
          </div>
        )}
      </div>

      <style>{`
        @keyframes slideInRight {
          from {
            transform: translateX(100%);
          }
          to {
            transform: translateX(0);
          }
        }
      `}</style>
    </div>
  );
}

/** OG Preview Card Component */
function OGPreviewCard({
  title,
  description,
  image,
  url,
}: {
  title: string;
  description: string;
  image?: string;
  url: string;
}) {
  return (
    <div className="border border-[oklch(30%_0.015_260)] rounded-xl bg-[oklch(15%_0.015_260)] overflow-hidden">
      {/* Image */}
      {image ? (
        <div className="aspect-video bg-[oklch(25%_0.015_260)]">
          <img src={image} alt="" className="w-full h-full object-cover" />
        </div>
      ) : (
        <div className="aspect-video bg-[oklch(25%_0.015_260)] flex items-center justify-center">
          <svg width="48" height="48" viewBox="0 0 48 48" fill="none" className="text-[oklch(40%_0_0)]">
            <rect x="8" y="8" width="32" height="32" rx="4" stroke="currentColor" strokeWidth="2"/>
            <circle cx="18" cy="18" r="4" stroke="currentColor" strokeWidth="2"/>
            <path d="M8 32l10-8 8 6 14-12" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
        </div>
      )}

      {/* Content */}
      <div className="p-3">
        <p className="text-[14px] font-medium text-[oklch(90%_0_0)] truncate">{title}</p>
        <p className="text-[12px] text-[oklch(55%_0_0)] truncate mt-0.5">{url}</p>
        <p className="text-[12px] text-[oklch(55%_0_0)] line-clamp-2 mt-1">{description}</p>

        {/* Badges */}
        <div className="flex gap-1.5 mt-3">
          <span className="bg-[oklch(55%_0.12_275)]/20 text-[oklch(55%_0.12_275)] text-xs px-2 py-0.5 rounded font-medium">
            OG
          </span>
          <span className="bg-[oklch(25%_0.015_260)] text-[oklch(55%_0_0)] text-xs px-2 py-0.5 rounded">
            X
          </span>
        </div>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>test -f frontend/src/components/Editor/SEOPanel.tsx && echo "EXISTS"</automated>
  </verify>
  <done>SEOPanel.tsx created with Meta Tags and robots.txt tabs, OG Preview card, and proper API integration</done>
</task>

<task type="auto">
  <name>Task 3: Add SEO button to EditorToolbar</name>
  <files>frontend/src/components/Editor/EditorToolbar.tsx</files>
  <read_first>
    frontend/src/components/Editor/EditorToolbar.tsx
  </read_first>
  <action>
    Add SEO button to EditorToolbar.tsx, positioned before the Publish button with a vertical divider.

Find the Publish button in the toolbar and add the SEO button before it.

The SEO button should:
1. Import SEOPanel component
2. Add state for isSeoPanelOpen
3. Render SEOPanel when isSeoPanelOpen is true
4. Add SEO icon button with onClick handler to open the panel
5. Pass blog data (id, metaTitle, metaDescription, username, coverImage) to SEOPanel

Example structure to add near the Publish button:

```tsx
// State
const [isSeoPanelOpen, setIsSeoPanelOpen] = useState(false);

// SEO Button (add before Publish button, with divider)
{/* Vertical divider */}
<div className="w-px h-6 bg-[oklch(30%_0.015_260)]" />

{/* SEO Button */}
<button
  onClick={() => setIsSeoPanelOpen(true)}
  className="p-2 text-[oklch(55%_0_0)] hover:bg-[oklch(25%_0.015_260)] hover:text-[oklch(90%_0_0)] rounded-lg transition-colors"
  title="SEO Settings"
>
  <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
    <circle cx="9" cy="9" r="6" stroke="currentColor" strokeWidth="1.5"/>
    <path d="M13 13l4 4" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/>
  </svg>
</button>

// SEOPanel (at end of component, inside the main container div)
<SEOPanel
  blogId={/* blog.id */}
  initialMetaTitle={/* blog.metaTitle */}
  initialMetaDescription={/* blog.metaDescription */}
  username={/* user?.username */}
  coverImage={/* blog.coverImage */}
  siteUrl={/* TODO: get from config or environment */}
  isOpen={isSeoPanelOpen}
  onClose={() => setIsSeoPanelOpen(false)}
/>
```

Note: The SEOPanel should be placed at the end of the toolbar's flex container to ensure correct positioning.
  </action>
  <verify>
    <automated>grep -c "SEOPanel" frontend/src/components/Editor/EditorToolbar.tsx && grep -c "SEO" frontend/src/components/Editor/EditorToolbar.tsx</automated>
  </verify>
  <done>SEO button added to EditorToolbar with SEOPanel integration</done>
</task>

</tasks>

<verification>
- api.ts has updateBlogSeo function
- api.ts has updateRobotsTxt function
- SEOPanel.tsx renders slide-over with Meta Tags and robots.txt tabs
- SEOPanel has OG Preview card
- SEOPanel handleSaveRobots calls updateRobotsTxt API
- EditorToolbar has SEO button that opens SEOPanel
- Auto-save with 500ms debounce works for meta fields
- Escape key closes panel
</verification>

<success_criteria>
- SEO button appears in editor toolbar before Publish button
- Clicking SEO button opens slide-over panel from right
- Panel has two tabs: Meta Tags and robots.txt
- Meta title input has 60 char limit with live counter
- Meta description textarea has 160 char limit with live counter
- Amber warning message visible in Meta Tags tab
- OG Preview card shows title, description, image (or placeholder)
- Panel closes via X button or Escape key
- robots.txt Save button calls PUT /api/user/robots
</success_criteria>

<output>
After completion, create `.planning/phases/12-seo-meta-tags-and-open-graph/12-04-SUMMARY.md`
</output>
