---
phase: 11-analytics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/schema.sql
  - backend/src/main/java/com/onepage/model/PageView.java
  - backend/src/main/java/com/onepage/model/BlogDailyStats.java
  - backend/src/main/java/com/onepage/mapper/PageViewMapper.java
  - backend/src/main/java/com/onepage/mapper/BlogDailyStatsMapper.java
  - backend/src/main/java/com/onepage/dto/AnalyticsDTO.java
autonomous: true
requirements:
  - ANAL-01
  - ANAL-02

must_haves:
  truths:
    - "User can view visitor counts for their published site"
    - "User can view page views per published site"
    - "Analytics data is stored in database for retrieval"
  artifacts:
    - path: "backend/src/main/resources/schema.sql"
      provides: "Database tables for analytics storage"
      contains: "CREATE TABLE.*page_views"
    - path: "backend/src/main/java/com/onepage/model/PageView.java"
      provides: "Page view entity mapping"
      contains: "@TableName"
    - path: "backend/src/main/java/com/onepage/model/BlogDailyStats.java"
      provides: "Daily stats entity mapping"
      contains: "@TableName"
    - path: "backend/src/main/java/com/onepage/mapper/PageViewMapper.java"
      provides: "Page view data access"
      exports: ["BaseMapper<PageView>"]
    - path: "backend/src/main/java/com/onepage/mapper/BlogDailyStatsMapper.java"
      provides: "Daily stats data access"
      exports: ["BaseMapper<BlogDailyStats>"]
    - path: "backend/src/main/java/com/onepage/dto/AnalyticsDTO.java"
      provides: "Analytics response data structure"
      exports: ["AnalyticsDTO", "AnalyticsDTO.DailyStat"]
  key_links:
    - from: "backend/src/main/java/com/onepage/model/PageView.java"
      to: "page_views table"
      via: "MyBatis-Plus @TableName annotation"
    - from: "backend/src/main/java/com/onepage/model/BlogDailyStats.java"
      to: "blog_daily_stats table"
      via: "MyBatis-Plus @TableName annotation"
    - from: "backend/src/main/java/com/onepage/dto/AnalyticsDTO.java"
      to: "AnalyticsController"
      via: "return type of getBlogStats()"
---

<objective>
Create the analytics data layer: database tables, entities, and MyBatis-Plus mappers for storing page views and daily aggregated statistics.
</objective>

<context>
@backend/src/main/resources/schema.sql
@backend/src/main/java/com/onepage/model/Blog.java
@backend/src/main/java/com/onepage/mapper/BlogMapper.java
@backend/src/main/java/com/onepage/dto/Result.java

**Analytics database schema (per research):**
```sql
CREATE TABLE IF NOT EXISTS `page_views` (
    `id` BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `blog_id` BIGINT NOT NULL,
    `visitor_fingerprint` VARCHAR(64) NOT NULL,
    `visited_at` DATETIME NOT NULL,
    `referer` VARCHAR(500),
    `user_agent` VARCHAR(500),
    PRIMARY KEY (`id`),
    INDEX `idx_blog_visited` (`blog_id`, `visited_at`),
    INDEX `idx_visitor_fingerprint` (`visitor_fingerprint`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `blog_daily_stats` (
    `id` BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `blog_id` BIGINT NOT NULL,
    `stat_date` DATE NOT NULL,
    `page_views` INT DEFAULT 0,
    `unique_visitors` INT DEFAULT 0,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_blog_date` (`blog_id`, `stat_date`),
    INDEX `idx_blog_id` (`blog_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database tables</name>
  <files>backend/src/main/resources/schema.sql</files>
  <read_first>
    backend/src/main/resources/schema.sql
  </read_first>
  <action>
    Add the following SQL statements at the end of schema.sql (before the INSERT statements if any):

```sql
-- Page views raw data
CREATE TABLE IF NOT EXISTS `page_views` (
    `id` BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `blog_id` BIGINT NOT NULL,
    `visitor_fingerprint` VARCHAR(64) NOT NULL COMMENT 'SHA-256 hash of IP + User-Agent',
    `visited_at` DATETIME NOT NULL,
    `referer` VARCHAR(500),
    `user_agent` VARCHAR(500),
    PRIMARY KEY (`id`),
    INDEX `idx_blog_visited` (`blog_id`, `visited_at`),
    INDEX `idx_visitor_fingerprint` (`visitor_fingerprint`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- Daily aggregation table
CREATE TABLE IF NOT EXISTS `blog_daily_stats` (
    `id` BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `blog_id` BIGINT NOT NULL,
    `stat_date` DATE NOT NULL,
    `page_views` INT DEFAULT 0,
    `unique_visitors` INT DEFAULT 0,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_blog_date` (`blog_id`, `stat_date`),
    INDEX `idx_blog_id` (`blog_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE.*page_views" backend/src/main/resources/schema.sql && grep -c "CREATE TABLE.*blog_daily_stats" backend/src/main/resources/schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - schema.sql contains "CREATE TABLE IF NOT EXISTS \`page_views\`"
    - schema.sql contains "CREATE TABLE IF NOT EXISTS \`blog_daily_stats\`"
    - schema.sql contains "visitor_fingerprint" column
    - schema.sql contains "stat_date" column for blog_daily_stats
  </acceptance_criteria>
  <done>Database tables added to schema.sql</done>
</task>

<task type="auto">
  <name>Task 2: Create PageView entity</name>
  <files>backend/src/main/java/com/onepage/model/PageView.java</files>
  <read_first>
    backend/src/main/java/com/onepage/model/Blog.java
  </read_first>
  <action>
    Create new file `backend/src/main/java/com/onepage/model/PageView.java`:

```java
package com.onepage.model;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

import java.time.LocalDateTime;

@Data
@TableName("page_views")
public class PageView {

    @TableId(type = IdType.AUTO)
    private Long id;

    private Long blogId;

    private String visitorFingerprint;

    private LocalDateTime visitedAt;

    private String referer;

    private String userAgent;
}
```
  </action>
  <verify>
    <automated>test -f backend/src/main/java/com/onepage/model/PageView.java && echo "EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - PageView.java exists at correct path
    - Class has @TableName("page_views") annotation
    - Class has IdType.AUTO primary key
    - Fields: blogId, visitorFingerprint, visitedAt, referer, userAgent
  </acceptance_criteria>
  <done>PageView entity created</done>
</task>

<task type="auto">
  <name>Task 3: Create BlogDailyStats entity</name>
  <files>backend/src/main/java/com/onepage/model/BlogDailyStats.java</files>
  <read_first>
    backend/src/main/java/com/onepage/model/Blog.java
  </read_first>
  <action>
    Create new file `backend/src/main/java/com/onepage/model/BlogDailyStats.java`:

```java
package com.onepage.model;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

import java.time.LocalDate;
import java.time.LocalDateTime;

@Data
@TableName("blog_daily_stats")
public class BlogDailyStats {

    @TableId(type = IdType.AUTO)
    private Long id;

    private Long blogId;

    private LocalDate statDate;

    private Integer pageViews;

    private Integer uniqueVisitors;

    private LocalDateTime createdAt;
}
```
  </action>
  <verify>
    <automated>test -f backend/src/main/java/com/onepage/model/BlogDailyStats.java && echo "EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - BlogDailyStats.java exists at correct path
    - Class has @TableName("blog_daily_stats") annotation
    - Fields: blogId, statDate (LocalDate), pageViews, uniqueVisitors, createdAt
  </acceptance_criteria>
  <done>BlogDailyStats entity created</done>
</task>

<task type="auto">
  <name>Task 4: Create PageViewMapper</name>
  <files>backend/src/main/java/com/onepage/mapper/PageViewMapper.java</files>
  <read_first>
    backend/src/main/java/com/onepage/mapper/BlogMapper.java
  </read_first>
  <action>
    Create new file `backend/src/main/java/com/onepage/mapper/PageViewMapper.java`:

```java
package com.onepage.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.onepage.model.PageView;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface PageViewMapper extends BaseMapper<PageView> {
}
```
  </action>
  <verify>
    <automated>test -f backend/src/main/java/com/onepage/mapper/PageViewMapper.java && echo "EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - PageViewMapper.java exists at correct path
    - Extends BaseMapper<PageView>
    - Has @Mapper annotation
  </acceptance_criteria>
  <done>PageViewMapper created</done>
</task>

<task type="auto">
  <name>Task 5: Create BlogDailyStatsMapper</name>
  <files>backend/src/main/java/com/onepage/mapper/BlogDailyStatsMapper.java</files>
  <read_first>
    backend/src/main/java/com/onepage/mapper/BlogMapper.java
  </read_first>
  <action>
    Create new file `backend/src/main/java/com/onepage/mapper/BlogDailyStatsMapper.java`:

```java
package com.onepage.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.onepage.model.BlogDailyStats;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface BlogDailyStatsMapper extends BaseMapper<BlogDailyStats> {
}
```
  </action>
  <verify>
    <automated>test -f backend/src/main/java/com/onepage/mapper/BlogDailyStatsMapper.java && echo "EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - BlogDailyStatsMapper.java exists at correct path
    - Extends BaseMapper<BlogDailyStats>
    - Has @Mapper annotation
  </acceptance_criteria>
  <done>BlogDailyStatsMapper created</done>
</task>

<task type="auto">
  <name>Task 6: Create AnalyticsDTO</name>
  <files>backend/src/main/java/com/onepage/dto/AnalyticsDTO.java</files>
  <read_first>
    backend/src/main/java/com/onepage/dto/Result.java
  </read_first>
  <action>
    Create new file `backend/src/main/java/com/onepage/dto/AnalyticsDTO.java`:

```java
package com.onepage.dto;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.util.List;

@Data
@NoArgsConstructor
@AllArgsConstructor
public class AnalyticsDTO {

    private Long blogId;

    private String blogTitle;

    private Integer totalPageViews;

    private Integer totalUniqueVisitors;

    private List<DailyStat> dailyStats;

    @Data
    @NoArgsConstructor
    @AllArgsConstructor
    public static class DailyStat {
        private String date;
        private Integer pageViews;
        private Integer uniqueVisitors;
    }
}
```
  </action>
  <verify>
    <automated>test -f backend/src/main/java/com/onepage/dto/AnalyticsDTO.java && echo "EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - AnalyticsDTO.java exists at correct path
    - Has totalPageViews, totalUniqueVisitors fields
    - Has nested DailyStat class
    - Has blogId and blogTitle fields
  </acceptance_criteria>
  <done>AnalyticsDTO created</done>
</task>

</tasks>

<verification>
- All 6 tasks complete
- Database schema updated
- 2 entities created (PageView, BlogDailyStats)
- 2 mappers created (PageViewMapper, BlogDailyStatsMapper)
- 1 DTO created (AnalyticsDTO)
</verification>

<success_criteria>
- Page views table exists in schema.sql with correct indexes
- Blog daily stats table exists in schema.sql with unique constraint on (blog_id, stat_date)
- PageView entity maps to page_views table
- BlogDailyStats entity maps to blog_daily_stats table
- Both mappers extend BaseMapper from MyBatis-Plus
- AnalyticsDTO has correct structure for API responses
</success_criteria>

<output>
After completion, create `.planning/phases/11-analytics/11-01-SUMMARY.md`
</output>
