---
phase: "15-analytics-data-layer"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/schema.sql
  - backend/src/main/java/com/onepage/util/RefererParser.java
  - backend/src/main/java/com/onepage/model/PageView.java
  - backend/src/main/java/com/onepage/model/BlogDailySourceStats.java
  - backend/src/main/java/com/onepage/mapper/BlogDailySourceStatsMapper.java
  - backend/src/main/java/com/onepage/service/AnalyticsService.java
autonomous: true
requirements:
  - ANLT-01
  - ANLT-02

must_haves:
  truths:
    - "RefererParser categorizes referer URLs into Direct, Search Engine, Social, Referral, Other"
    - "PageView entity stores refererSource field when recording a page view"
    - "AnalyticsService.recordPageView() persists the categorized source to database"
    - "BlogDailySourceStats entity and mapper exist for daily source aggregation"
  artifacts:
    - path: "backend/src/main/java/com/onepage/util/RefererParser.java"
      provides: "Source categorization utility"
      contains: "enum Source"
    - path: "backend/src/main/java/com/onepage/model/PageView.java"
      provides: "Page view entity with refererSource"
      contains: "refererSource"
    - path: "backend/src/main/java/com/onepage/model/BlogDailySourceStats.java"
      provides: "Daily source aggregation entity"
      contains: "@TableName.*blog_daily_source_stats"
    - path: "backend/src/main/java/com/onepage/mapper/BlogDailySourceStatsMapper.java"
      provides: "Daily source stats data access"
      exports: ["BaseMapper<BlogDailySourceStats>"]
    - path: "backend/src/main/java/com/onepage/service/AnalyticsService.java"
      provides: "Analytics recording with source categorization"
      contains: "RefererParser.categorize"
    - path: "backend/src/main/resources/schema.sql"
      provides: "Database schema for source tracking"
      contains: "referer_source"
---

<objective>
Build the analytics data layer for enhanced analytics (v1.5): a RefererParser utility to categorize referer URLs, modifications to store categorized source in PageView, and BlogDailySourceStats entity and mapper for daily source aggregation.
</objective>

<context>
@backend/src/main/java/com/onepage/model/PageView.java
@backend/src/main/java/com/onepage/service/AnalyticsService.java
@backend/src/main/java/com/onepage/model/BlogDailyStats.java
@backend/src/main/java/com/onepage/mapper/BlogDailyStatsMapper.java
@backend/src/main/resources/schema.sql

**Phase 15 adds:**
1. `RefererParser` utility - categorizes referer URLs into Source enum (DIRECT, SEARCH_ENGINE, SOCIAL, REFERRAL, OTHER)
2. `referer_source` column in `page_views` table
3. `BlogDailySourceStats` entity and mapper for daily source pre-aggregation
4. `AnalyticsService.recordPageView()` modification to store categorized source
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQL schema for referer_source column and blog_daily_source_stats table</name>
  <files>backend/src/main/resources/schema.sql</files>
  <read_first>
    backend/src/main/resources/schema.sql
  </read_first>
  <action>
    Append the following SQL to schema.sql after the existing page_views and blog_daily_stats table definitions (around line 125):

```sql
-- Add referer_source column to page_views for source categorization
ALTER TABLE `page_views`
ADD COLUMN `referer_source` VARCHAR(20) DEFAULT NULL
COMMENT 'DIRECT, SEARCH_ENGINE, SOCIAL, REFERRAL, OTHER'
AFTER `referer`;

-- Add index for aggregation queries on source
CREATE INDEX `idx_blog_visited_source` ON `page_views` (`blog_id`, `visited_at`, `referer_source`);

-- Daily source aggregation table for pre-computed source stats
CREATE TABLE IF NOT EXISTS `blog_daily_source_stats` (
    `id` BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `blog_id` BIGINT NOT NULL,
    `stat_date` DATE NOT NULL,
    `source` VARCHAR(20) NOT NULL COMMENT 'DIRECT, SEARCH_ENGINE, SOCIAL, REFERRAL, OTHER',
    `page_views` INT DEFAULT 0,
    `unique_visitors` INT DEFAULT 0,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_blog_date_source` (`blog_id`, `stat_date`, `source`),
    INDEX `idx_blog_id` (`blog_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```
  </action>
  <verify>
    <automated>grep -c "referer_source" backend/src/main/resources/schema.sql && grep -c "blog_daily_source_stats" backend/src/main/resources/schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - schema.sql contains "ALTER TABLE.*page_views.*ADD COLUMN.*referer_source"
    - schema.sql contains "CREATE INDEX.*idx_blog_visited_source.*blog_id.*visited_at.*referer_source"
    - schema.sql contains "CREATE TABLE.*blog_daily_source_stats"
    - schema.sql contains "UNIQUE KEY.*uk_blog_date_source.*blog_id.*stat_date.*source"
  </acceptance_criteria>
  <done>SQL schema updated with referer_source column and blog_daily_source_stats table</done>
</task>

<task type="auto">
  <name>Task 2: Create RefererParser utility</name>
  <files>backend/src/main/java/com/onepage/util/RefererParser.java</files>
  <read_first>
    backend/src/main/java/com/onepage/util/JwtUtil.java
  </read_first>
  <action>
    Create new file `backend/src/main/java/com/onepage/util/RefererParser.java`:

```java
package com.onepage.util;

import lombok.extern.slf4j.Slf4j;

import java.util.Set;

/**
 * Utility for categorizing HTTP referer URLs into standard analytics source categories.
 * Categories: DIRECT, SEARCH_ENGINE, SOCIAL, REFERRAL, OTHER.
 */
@Slf4j
public class RefererParser {

    private static final Set<String> SEARCH_ENGINE_DOMAINS = Set.of(
        "google.com", "www.google.com", "google.co.uk", "google.cn",
        "google.com.hk", "google.de", "google.fr", "google.jp",
        "bing.com", "www.bing.com",
        "baidu.com", "www.baidu.com",
        "yahoo.com", "www.yahoo.com", "yahoo.co.jp",
        "yandex.com", "www.yandex.com", "yandex.ru",
        "sogou.com", "www.sogou.com",
        "so.com", "www.so.com",
        "360.cn", "www.360.cn"
    );

    private static final Set<String> SOCIAL_DOMAINS = Set.of(
        "twitter.com", "www.twitter.com", "x.com",
        "facebook.com", "www.facebook.com",
        "instagram.com", "www.instagram.com",
        "linkedin.com", "www.linkedin.com",
        "weibo.com", "www.weibo.com",
        "reddit.com", "www.reddit.com",
        "pinterest.com", "www.pinterest.com",
        "douban.com", "www.douban.com",
        "zhihu.com", "www.zhihu.com",
        "douyin.com", "www.douyin.com",
        "xiaohongshu.com", "www.xiaohongshu.com",
        "threads.net", "www.threads.net",
        "tiktok.com", "www.tiktok.com"
    );

    public enum Source {
        DIRECT("Direct"),
        SEARCH_ENGINE("Search Engine"),
        SOCIAL("Social"),
        REFERRAL("Referral"),
        OTHER("Other");

        private final String displayName;

        Source(String displayName) {
            this.displayName = displayName;
        }

        public String getDisplayName() {
            return displayName;
        }
    }

    /**
     * Categorize a referer URL into a Source enum value.
     * Returns DIRECT for null/empty referer (direct traffic).
     */
    public static Source categorize(String referer) {
        if (referer == null || referer.isEmpty()) {
            return Source.DIRECT;
        }

        try {
            String domain = extractDomain(referer);
            if (domain == null) {
                return Source.OTHER;
            }

            String lowerDomain = domain.toLowerCase();

            // Check search engines
            if (isSearchEngine(lowerDomain)) {
                return Source.SEARCH_ENGINE;
            }

            // Check social media
            if (isSocialMedia(lowerDomain)) {
                return Source.SOCIAL;
            }

            // Otherwise it's a referral
            return Source.REFERRAL;

        } catch (Exception e) {
            log.warn("Failed to parse referer: {}", referer, e);
            return Source.OTHER;
        }
    }

    private static boolean isSearchEngine(String domain) {
        return SEARCH_ENGINE_DOMAINS.stream().anyMatch(d ->
            domain.equals(d) || domain.endsWith("." + d));
    }

    private static boolean isSocialMedia(String domain) {
        return SOCIAL_DOMAINS.stream().anyMatch(d ->
            domain.equals(d) || domain.endsWith("." + d));
    }

    private static String extractDomain(String url) {
        if (url.startsWith("//")) {
            url = "https:" + url;
        }
        if (!url.startsWith("http://") && !url.startsWith("https://")) {
            url = "https://" + url;
        }
        try {
            java.net.URL parsed = new java.net.URL(url);
            return parsed.getHost();
        } catch (Exception e) {
            return null;
        }
    }
}
```
  </action>
  <verify>
    <automated>test -f backend/src/main/java/com/onepage/util/RefererParser.java && grep -c "enum Source" backend/src/main/java/com/onepage/util/RefererParser.java</automated>
  </verify>
  <acceptance_criteria>
    - RefererParser.java exists at correct path
    - Class has public enum Source with values: DIRECT, SEARCH_ENGINE, SOCIAL, REFERRAL, OTHER
    - Source enum has getDisplayName() method
    - Public static method categorize(String) returns Source enum
    - Handles null/empty referer returning DIRECT
    - SEARCH_ENGINE_DOMAINS contains google.com, bing.com, baidu.com
    - SOCIAL_DOMAINS contains twitter.com, facebook.com, weibo.com
  </acceptance_criteria>
  <done>RefererParser utility created</done>
</task>

<task type="auto">
  <name>Task 3: Modify PageView entity to add refererSource field</name>
  <files>backend/src/main/java/com/onepage/model/PageView.java</files>
  <read_first>
    backend/src/main/java/com/onepage/model/PageView.java
  </read_first>
  <action>
    Add the refererSource field to PageView.java after line 23 (after `private String referer;`):

```java
    private String referer;

    private String refererSource;  // DIRECT, SEARCH_ENGINE, SOCIAL, REFERRAL, OTHER
```
  </action>
  <verify>
    <automated>grep -c "refererSource" backend/src/main/java/com/onepage/model/PageView.java</automated>
  </verify>
  <acceptance_criteria>
    - PageView.java contains "private String refererSource;"
    - Field has comment explaining valid values: DIRECT, SEARCH_ENGINE, SOCIAL, REFERRAL, OTHER
  </acceptance_criteria>
  <done>PageView entity updated with refererSource field</done>
</task>

<task type="auto">
  <name>Task 4: Create BlogDailySourceStats entity</name>
  <files>backend/src/main/java/com/onepage/model/BlogDailySourceStats.java</files>
  <read_first>
    backend/src/main/java/com/onepage/model/BlogDailyStats.java
  </read_first>
  <action>
    Create new file `backend/src/main/java/com/onepage/model/BlogDailySourceStats.java`:

```java
package com.onepage.model;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

import java.time.LocalDate;
import java.time.LocalDateTime;

@Data
@TableName("blog_daily_source_stats")
public class BlogDailySourceStats {

    @TableId(type = IdType.AUTO)
    private Long id;

    private Long blogId;

    private LocalDate statDate;

    private String source;  // DIRECT, SEARCH_ENGINE, SOCIAL, REFERRAL, OTHER

    private Integer pageViews;

    private Integer uniqueVisitors;

    private LocalDateTime createdAt;
}
```
  </action>
  <verify>
    <automated>test -f backend/src/main/java/com/onepage/model/BlogDailySourceStats.java && grep -c "@TableName.*blog_daily_source_stats" backend/src/main/java/com/onepage/model/BlogDailySourceStats.java</automated>
  </verify>
  <acceptance_criteria>
    - BlogDailySourceStats.java exists at correct path
    - Class has @TableName("blog_daily_source_stats") annotation
    - Fields: id (auto), blogId, statDate (LocalDate), source, pageViews, uniqueVisitors, createdAt
    - Uses IdType.AUTO for primary key
  </acceptance_criteria>
  <done>BlogDailySourceStats entity created</done>
</task>

<task type="auto">
  <name>Task 5: Create BlogDailySourceStatsMapper</name>
  <files>backend/src/main/java/com/onepage/mapper/BlogDailySourceStatsMapper.java</files>
  <read_first>
    backend/src/main/java/com/onepage/mapper/BlogDailyStatsMapper.java
  </read_first>
  <action>
    Create new file `backend/src/main/java/com/onepage/mapper/BlogDailySourceStatsMapper.java`:

```java
package com.onepage.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.onepage.model.BlogDailySourceStats;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface BlogDailySourceStatsMapper extends BaseMapper<BlogDailySourceStats> {
}
```
  </action>
  <verify>
    <automated>test -f backend/src/main/java/com/onepage/mapper/BlogDailySourceStatsMapper.java && grep -c "BaseMapper<BlogDailySourceStats>" backend/src/main/java/com/onepage/mapper/BlogDailySourceStatsMapper.java</automated>
  </verify>
  <acceptance_criteria>
    - BlogDailySourceStatsMapper.java exists at correct path
    - Extends BaseMapper<BlogDailySourceStats>
    - Has @Mapper annotation
  </acceptance_criteria>
  <done>BlogDailySourceStatsMapper created</done>
</task>

<task type="auto">
  <name>Task 6: Modify AnalyticsService.recordPageView() to store categorized source</name>
  <files>backend/src/main/java/com/onepage/service/AnalyticsService.java</files>
  <read_first>
    backend/src/main/java/com/onepage/service/AnalyticsService.java
  </read_first>
  <action>
    In AnalyticsService.java, add import for RefererParser:

```java
import com.onepage.util.RefererParser;
```

Then modify the recordPageView method (around lines 53-60) to call RefererParser.categorize() and set the refererSource:

Find this existing code:
```java
            // Save raw page view
            PageView pageView = new PageView();
            pageView.setBlogId(blogId);
            pageView.setVisitorFingerprint(fingerprint);
            pageView.setVisitedAt(now);
            pageView.setUserAgent(truncate(userAgent, 500));
            pageView.setReferer(truncate(referer, 500));
            pageViewMapper.insert(pageView);
```

Replace with:
```java
            // Parse referer to categorize source
            RefererParser.Source source = RefererParser.categorize(referer);

            // Save raw page view
            PageView pageView = new PageView();
            pageView.setBlogId(blogId);
            pageView.setVisitorFingerprint(fingerprint);
            pageView.setVisitedAt(now);
            pageView.setUserAgent(truncate(userAgent, 500));
            pageView.setReferer(truncate(referer, 500));
            pageView.setRefererSource(source.name());  // Store categorized source
            pageViewMapper.insert(pageView);
```
  </action>
  <verify>
    <automated>grep -c "RefererParser.categorize" backend/src/main/java/com/onepage/service/AnalyticsService.java && grep -c "setRefererSource" backend/src/main/java/com/onepage/service/AnalyticsService.java</automated>
  </verify>
  <acceptance_criteria>
    - AnalyticsService.java imports RefererParser
    - recordPageView() calls RefererParser.categorize(referer)
    - PageView.setRefererSource() is called with source.name()
  </acceptance_criteria>
  <done>AnalyticsService.recordPageView() modified to store categorized source</done>
</task>

<task type="auto">
  <name>Task 7: Create RefererParserTest unit tests</name>
  <files>backend/src/test/java/com/onepage/util/RefererParserTest.java</files>
  <read_first>
    backend/src/test/java/com/onepage/service/FulfillmentServiceTest.java
  </read_first>
  <action>
    Create new file `backend/src/test/java/com/onepage/util/RefererParserTest.java`:

```java
package com.onepage.util;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.CsvSource;
import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.jupiter.api.extension.Extensions;
import org.junit.platform.runner.JUnitPlatform;
import org.junit.runner.RunWith;

import static org.junit.jupiter.api.Assertions.*;

@RunWith(JUnitPlatform.class)
class RefererParserTest {

    @Test
    void categorize_returnsDirectForNull() {
        assertEquals(RefererParser.Source.DIRECT, RefererParser.categorize(null));
    }

    @Test
    void categorize_returnsDirectForEmpty() {
        assertEquals(RefererParser.Source.DIRECT, RefererParser.categorize(""));
    }

    @ParameterizedTest
    @CsvSource({
        "https://www.google.com, SEARCH_ENGINE",
        "https://google.com, SEARCH_ENGINE",
        "https://bing.com/search, SEARCH_ENGINE",
        "https://www.bing.com, SEARCH_ENGINE",
        "https://baidu.com/s, SEARCH_ENGINE",
        "https://www.baidu.com, SEARCH_ENGINE",
        "https://yandex.ru/search, SEARCH_ENGINE",
        "https://www.bing.com/search?q=test, SEARCH_ENGINE"
    })
    void categorize_identifiesSearchEngines(String url, String expected) {
        assertEquals(RefererParser.Source.valueOf(expected), RefererParser.categorize(url));
    }

    @ParameterizedTest
    @CsvSource({
        "https://twitter.com, SOCIAL",
        "https://www.twitter.com, SOCIAL",
        "https://x.com, SOCIAL",
        "https://www.facebook.com, SOCIAL",
        "https://www.instagram.com, SOCIAL",
        "https://www.linkedin.com, SOCIAL",
        "https://weibo.com, SOCIAL",
        "https://www.weibo.com, SOCIAL",
        "https://reddit.com/r/java, SOCIAL",
        "https://www.reddit.com, SOCIAL",
        "https://www.douyin.com, SOCIAL",
        "https://www.tiktok.com, SOCIAL"
    })
    void categorize_identifiesSocialMedia(String url, String expected) {
        assertEquals(RefererParser.Source.valueOf(expected), RefererParser.categorize(url));
    }

    @Test
    void categorize_identifiesReferrals() {
        // A regular website referer that is not search engine or social
        assertEquals(RefererParser.Source.REFERRAL, RefererParser.categorize("https://example.com"));
        assertEquals(RefererParser.Source.REFERRAL, RefererParser.categorize("https://blog.example.com"));
        assertEquals(RefererParser.Source.REFERRAL, RefererParser.categorize("https://news.ycombinator.com/item?id=123"));
    }

    @Test
    void categorize_returnsOtherForInvalidUrls() {
        assertEquals(RefererParser.Source.OTHER, RefererParser.categorize("not-a-url"));
        assertEquals(RefererParser.Source.OTHER, RefererParser.categorize("javascript:void(0)"));
    }

    @Test
    void source_getDisplayName_returnsCorrectNames() {
        assertEquals("Direct", RefererParser.Source.DIRECT.getDisplayName());
        assertEquals("Search Engine", RefererParser.Source.SEARCH_ENGINE.getDisplayName());
        assertEquals("Social", RefererParser.Source.SOCIAL.getDisplayName());
        assertEquals("Referral", RefererParser.Source.REFERRAL.getDisplayName());
        assertEquals("Other", RefererParser.Source.OTHER.getDisplayName());
    }
}
```
  </action>
  <verify>
    <automated>test -f backend/src/test/java/com/onepage/util/RefererParserTest.java && grep -c "@RunWith" backend/src/test/java/com/onepage/util/RefererParserTest.java</automated>
  </verify>
  <acceptance_criteria>
    - RefererParserTest.java exists at correct path
    - Tests null and empty referer return DIRECT
    - Tests search engine URLs return SEARCH_ENGINE
    - Tests social media URLs return SOCIAL
    - Tests regular URLs return REFERRAL
    - Tests invalid URLs return OTHER
    - Tests Source.getDisplayName() returns correct display names
  </acceptance_criteria>
  <done>RefererParserTest unit tests created</done>
</task>

</tasks>

<verification>
- Task 1: SQL schema updated with referer_source column and blog_daily_source_stats table
- Task 2: RefererParser.java created with Source enum and categorize() method
- Task 3: PageView.java modified with refererSource field
- Task 4: BlogDailySourceStats.java entity created
- Task 5: BlogDailySourceStatsMapper.java created
- Task 6: AnalyticsService.recordPageView() calls RefererParser.categorize() and stores result
- Task 7: RefererParserTest.java unit tests created
</verification>

<success_criteria>
- RefererParser categorizes referer URLs into: DIRECT, SEARCH_ENGINE, SOCIAL, REFERRAL, OTHER
- PageView entity has refererSource field (String)
- AnalyticsService.recordPageView() persists categorized source via pageView.setRefererSource(source.name())
- BlogDailySourceStats entity maps to blog_daily_source_stats table with fields: id, blogId, statDate, source, pageViews, uniqueVisitors, createdAt
- BlogDailySourceStatsMapper extends BaseMapper<BlogDailySourceStats>
- Database schema contains referer_source column in page_views and blog_daily_source_stats table
</success_criteria>

<output>
After completion, update STATE.md to mark Phase 15 as in progress.
</output>
