---
phase: 5
slug: polish-performance
plan: 03
wave: 2
depends_on: [05-01]
status: draft
created: 2026-03-21
requirements: [PERF-05]
---

# Plan 05-03: Async Job Processing - RabbitMQ for PDF and AI Generation

## Goal

Verify RabbitMQ async job processing is active for PDF generation and AI generation, ensuring these heavy operations do not block the main request thread.

## Context

RabbitMQ is already configured with:
- `blog.generate.queue` for AI generation
- `pdf.job.queue` for PDF generation

Consumers already exist:
- `PdfJobConsumer.java` - processes PDF generation jobs
- `GenerationMessageConsumer.java` - processes AI generation jobs

This plan verifies the async infrastructure is correctly wired and documents the async flow.

## Wave 1 Tasks

### Task 05-03-01: Verify PdfJobConsumer Exists and is Annotated

<read_first>
- `/Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/messaging/PdfJobConsumer.java` (expected to exist with @RabbitListener annotation)
</read_first>

<action>
Verify PdfJobConsumer exists with @RabbitListener annotation for pdf.job.queue.

Expected file location: `backend/src/main/java/com/onepage/messaging/PdfJobConsumer.java`

Expected annotations:
- @RabbitListener on class or method
- Queue name: pdf.job.queue (from RabbitMQConfig.PDF_JOB_QUEUE)
</action>

<acceptance_criteria>
- File `backend/src/main/java/com/onepage/messaging/PdfJobConsumer.java` exists
- `grep "@RabbitListener" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/messaging/PdfJobConsumer.java` returns at least 1 match
- `grep "pdf.job.queue" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/messaging/PdfJobConsumer.java` returns at least 1 match
- `grep "@Slf4j" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/messaging/PdfJobConsumer.java` returns at least 1 match
</acceptance_criteria>

---

### Task 05-03-02: Verify GenerationMessageConsumer Exists and is Annotated

<read_first>
- `/Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/messaging/GenerationMessageConsumer.java` (expected to exist with @RabbitListener annotation)
</read_first>

<action>
Verify GenerationMessageConsumer exists with @RabbitListener annotation for blog.generate.queue.

Expected file location: `backend/src/main/java/com/onepage/messaging/GenerationMessageConsumer.java`

Expected annotations:
- @RabbitListener on class or method
- Queue name: blog.generate.queue (from RabbitMQConfig.BLOG_GENERATE_QUEUE)
</action>

<acceptance_criteria>
- File `backend/src/main/java/com/onepage/messaging/GenerationMessageConsumer.java` exists
- `grep "@RabbitListener" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/messaging/GenerationMessageConsumer.java` returns at least 1 match
- `grep "blog.generate.queue" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/messaging/GenerationMessageConsumer.java` returns at least 1 match
</acceptance_criteria>

---

### Task 05-03-03: Verify RabbitMQ Queues are Declared

<read_first>
- `/Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/config/RabbitMQConfig.java` (existing config with queue declarations)
</read_first>

<action>
Verify RabbitMQConfig declares both queues:
- `blog.generate.queue` via blogGenerateQueue() method
- `pdf.job.queue` via pdfJobQueue() method

Expected constants in RabbitMQConfig:
- `BLOG_GENERATE_QUEUE = "blog.generate.queue"`
- `PDF_JOB_QUEUE = "pdf.job.queue"`
</action>

<acceptance_criteria>
- File `backend/src/main/java/com/onepage/config/RabbitMQConfig.java` exists
- `grep "BLOG_GENERATE_QUEUE" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/config/RabbitMQConfig.java` returns at least 1 match
- `grep "PDF_JOB_QUEUE" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/config/RabbitMQConfig.java` returns at least 1 match
- `grep "blogGenerateQueue" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/config/RabbitMQConfig.java` returns at least 1 match
- `grep "pdfJobQueue" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/config/RabbitMQConfig.java` returns at least 1 match
</acceptance_criteria>

---

### Task 05-03-04: Verify PdfJobProducer Sends Messages to Queue

<read_first>
- `/Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/messaging/PdfJobConsumer.java` (verify queue name constant)
</read_first>

<action>
Search for a producer class that sends messages to pdf.job.queue.

Look for:
- Class with name containing "PdfJobProducer" OR
- Method that references "pdf.job.queue" constant

Use grep to search:
```bash
grep -r "pdf.job.queue" backend/src/main/java/com/onepage/ --include="*.java"
```
</action>

<acceptance_criteria>
- `grep -r "pdf.job.queue" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/ --include="*.java"` returns at least 1 match in a producer file
- OR `grep -r "PdfJobProducer" /Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/ --include="*.java"` returns at least 1 match
</acceptance_criteria>

---

### Task 05-03-05: Document Async Flow in Performance Documentation

<read_first>
- `/Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/config/RabbitMQConfig.java` (queue names)
- `/Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/messaging/PdfJobConsumer.java` (PDF processing)
- `/Users/chunxiang/Desktop/Vibe/Onepage/backend/src/main/java/com/onepage/messaging/GenerationMessageConsumer.java` (AI processing)
</read_first>

<action>
Create new file `/Users/chunxiang/Desktop/Vibe/Onepage/docs/PERFORMANCE.md`:

```markdown
# Performance Architecture

## Overview

This document describes the performance optimizations implemented to handle 500 QPS on hot endpoints.

## Async Processing (PERF-05)

Heavy operations (PDF generation, AI generation) are processed asynchronously via RabbitMQ to prevent blocking the main request thread.

### PDF Generation Flow

1. Client calls `POST /api/payment/create` with PDF generation request
2. `PdfJobProducer.queuePdfGeneration()` sends message to `pdf.job.queue`
3. `PdfJobConsumer.processPdfJob()` consumes message asynchronously
4. PDF generated server-side, stored to disk
5. Client polls or receives WebSocket notification when complete

### AI Generation Flow

1. Client calls `POST /api/blog/generate` with image + description
2. `GenerationMessageProducer.sendGenerationRequest()` sends message to `blog.generate.queue`
3. `GenerationMessageConsumer.handleGenerationRequest()` consumes asynchronously
4. AI pipeline stages execute: Image Analysis -> Style Extraction -> Content Generation -> Block Assembly
5. WebSocket notifications sent via `SimpMessagingTemplate`

### Why Async?

- PDF generation can take 5-30 seconds
- AI generation can take 10-60 seconds
- Without async, these would block the HTTP request thread
- At 500 QPS, blocking would exhaust thread pool immediately

## Caching (PERF-02)

### Template Listing

Templates are cached in Redis with 24h TTL using Spring Cache abstraction.

- `GET /api/templates` -> `@Cacheable(value = "templates", key = "'all'")`
- Cache key pattern: `templates::all`, `templates::category:1`, etc.
- TTL: 86400000ms (24 hours)

### Blog Pages

Blogs are cached manually in Redis with 24h TTL.

- `GET /api/blog/share/{shareCode}` -> BlogService.getBlogByShareCode()
- Cache key pattern: `blog:share:{shareCode}`, `blog:{id}`
- TTL: 24 hours

## Connection Pool (PERF-04)

HikariCP is tuned for high concurrency:

- `maximum-pool-size: 50` - handles 50 concurrent DB connections
- `minimum-idle: 10` - maintains 10 idle connections
- `connection-timeout: 30000` - 30s timeout for acquiring connection

Sizing formula: `connections = ((core_count * 2) + effective_spindle_count)`

For MySQL with SSD: 50 connections is appropriate for 500 QPS.

## Database Indexes (PERF-03)

Indexes on frequently queried fields:

- `blogs.user_id` - INDEX `idx_user_id`
- `blogs.share_code` - INDEX `idx_share_code`

These indexes ensure fast lookups even under high load.

## Load Testing

JMeter test plans verify 500 QPS capability:

- `jmeter/blog-share-500qps.jmx` - Tests blog share endpoint
- `jmeter/templates-list-500qps.jmx` - Tests template listing endpoint

Run full test:
```bash
jmeter -n -t jmeter/blog-share-500qps.jmx -l jmeter/results.jtl -e -o jmeter/report
```

Success criteria:
- >99% success rate
- p99 latency < 500ms
- No connection pool exhaustion errors
```
</action>

<acceptance_criteria>
- File `/Users/chunxiang/Desktop/Vibe/Onepage/docs/PERFORMANCE.md` exists
- `grep "pdf.job.queue" /Users/chunxiang/Desktop/Vibe/Onepage/docs/PERFORMANCE.md` returns at least 1 match
- `grep "blog.generate.queue" /Users/chunxiang/Desktop/Vibe/Onepage/docs/PERFORMANCE.md` returns at least 1 match
- `grep "500 QPS" /Users/chunxiang/Desktop/Vibe/Onepage/docs/PERFORMANCE.md` returns at least 1 match
- `grep "maximum-pool-size: 50" /Users/chunxiang/Desktop/Vibe/Onepage/docs/PERFORMANCE.md` returns at least 1 match
</acceptance_criteria>

---

## Verification

After all tasks complete:

1. **Consumers exist:** `ls -la backend/src/main/java/com/onepage/messaging/*.java`
2. **Queues declared:** `grep "QUEUE" backend/src/main/java/com/onepage/config/RabbitMQConfig.java`
3. **Documentation created:** `cat docs/PERFORMANCE.md | head -50`

## must_haves

- PdfJobConsumer exists with @RabbitListener for pdf.job.queue
- GenerationMessageConsumer exists with @RabbitListener for blog.generate.queue
- RabbitMQConfig declares both queues
- PERFORMANCE.md documents async flow, caching, connection pool, and indexes
